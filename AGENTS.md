# 会议助手 - 项目规范文档

## 项目概览

**项目名称**: 会议助手  
**项目类型**: 跨平台 Web 应用（支持电脑、手机、平板）  
**核心功能**: 会议录音 → 实时转文字 → 词云分析

## 技术栈

| 技术 | 说明 |
|------|------|
| Framework | Next.js 16 (App Router) |
| Core | React 19 |
| Language | TypeScript 5 |
| UI 组件 | shadcn/ui + Tailwind CSS |
| 录音 | MediaRecorder API |
| 语音转文字 | ASR API (coze-coding-dev-sdk) |
| 词云权重分析 | LLM API (豆包模型) |
| 词云 | echarts-wordcloud |

## 核心功能流程

```
┌─────────────────────────────────────────────────────────────┐
│                    用户点击"会议开始"                          │
└─────────────────────────┬───────────────────────────────────┘
                          ▼
┌─────────────────────────────────────────────────────────────┐
│                   麦克风开始持续录音                           │
│  • 每 3 分钟保存一个音频片段                                  │
│  • 后台自动触发 ASR 转写                                     │
│  • 后台自动触发 LLM 权重分析                                  │
│  • 最大录音时长：2 小时                                       │
│  • 静默检测：30 分钟无声音自动停止                             │
└─────────────────────────┬───────────────────────────────────┘
                          ▼
┌─────────────────────────────────────────────────────────────┐
│                  用户点击"生成词云"                           │
└─────────────────────────┬───────────────────────────────────┘
                          ▼
┌─────────────────────────────────────────────────────────────┐
│         合并所有处理结果 → 绿色椭圆词云（带权重）              │
└─────────────────────────────────────────────────────────────┘
```

## API 路由

| 路由 | 方法 | 说明 |
|------|------|------|
| `/api/transcribe` | POST | 音频转文字（ASR） |
| `/api/analyze-words` | POST | 关键词权重分析（LLM） |

## 流水线处理

- **片段时长**：3 分钟
- **最大录音**：2 小时（约 40 个片段）
- **失败处理**：自动重试 1 次，再失败则跳过并显示警告

## 词云权重规则

| 类别 | 权重倍数 | 示例 |
|------|---------|------|
| 地理相关 | ×3 | 中国、世界、亚洲、城市、河流、山脉 |
| 教育相关 | ×2.5 | 学习、学生、老师、课程、知识 |
| 专业术语 | ×2 | 概念、原理、规律 |
| 一般词汇 | ×1 | 保持原词频 |
| 无意义虚词 | ×0.1 或过滤 | 这个、那个、就是 |

## 构建和测试命令

```bash
# 安装依赖
pnpm install

# 开发环境启动
pnpm dev

# 构建生产版本
pnpm build

# 代码检查
pnpm lint

# 类型检查
pnpm ts-check
```

## 目录结构

```
src/
├── app/
│   ├── page.tsx              # 主页面
│   ├── layout.tsx            # 布局组件
│   ├── globals.css           # 全局样式（绿色主题）
│   └── api/
│       ├── transcribe/       # ASR 转写 API
│       └── analyze-words/     # LLM 权重分析 API
├── components/
│   ├── MeetingRecorder.tsx   # 录音组件（流水线核心）
│   ├── TranscriptView.tsx    # 转写预览组件
│   └── WordCloud.tsx         # 词云组件
├── hooks/
│   └── useSpeechRecognition.ts  # 语音识别 hook（保留）
└── lib/
    └── utils.ts              # 工具函数
```

## 关键配置

### 静默检测配置
- 能量阈值: `0.02`
- 最大静默时间: `30分钟 (1800000ms)`

### 语音识别配置
- 语言: `zh-CN` (中文)
- 持续识别: `true`
- 音频采样率: `16000Hz`

### 词云配置
- 形状: 椭圆 (ellipse)
- 主题色: 绿色系 (#22c55e → #15803d)
- 最大关键词数: 100

## 响应式适配

| 设备 | 布局 | 字云尺寸 |
|------|------|---------|
| 手机 (< 640px) | 单列，垂直堆叠 | 100% 宽度 |
| 平板 (640-1024px) | 单列，保持比例 | 100% 宽度 |
| 电脑 (> 1024px) | 双栏，左录音右预览 | 固定高度 400px |

## 注意事项

1. **浏览器兼容性**: Web Speech API 仅在 Chrome、Safari、Edge 等现代浏览器中可用
2. **HTTPS 要求**: 录音功能需要在安全上下文 (HTTPS) 下运行
3. **麦克风权限**: 用户首次使用时需要授权麦克风访问权限
4. **页面保活**: 录音时最小化也能持续录制（静音音频保活）
