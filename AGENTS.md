# 会议助手 - 项目规范文档

## 项目概览

**项目名称**: 会议助手  
**项目类型**: 跨平台 Web 应用（支持电脑、手机、平板）  
**核心功能**: 会议录音 → 实时转文字 → 词云分析 / 粘贴文稿 → 词云分析

## 技术栈

| 技术 | 说明 |
|------|------|
| Framework | Next.js 16 (App Router) |
| Core | React 19 |
| Language | TypeScript 5 |
| UI 组件 | shadcn/ui + Tailwind CSS |
| 录音 | MediaRecorder API |
| 语音转文字 | ASR API (coze-coding-dev-sdk / ASRClient) |
| 词云权重分析 | LLM API (coze-coding-dev-sdk / LLMClient, doubao-seed-2-0-lite) |
| 对象存储 | S3Storage (coze-coding-dev-sdk), 音频文件上传 + 签名URL |
| 词云 | echarts-wordcloud |

## 核心功能流程

### 流程A：录音转词云

```
┌─────────────────────────────────────────────────────────────┐
│                    用户点击"会议开始"                          │
└─────────────────────────┬───────────────────────────────────┘
                          ▼
┌─────────────────────────────────────────────────────────────┐
│                   麦克风开始持续录音                           │
│  • 每 3 分钟保存一个音频片段（stop → onstop收集 → restart）   │
│  • 音频上传至对象存储 → 签名URL → ASR 转写                    │
│  • 后台自动触发 LLM 权重分析                                  │
│  • 最大录音时长：2 小时                                       │
│  • 静默检测：30 分钟无声音自动停止                             │
│  • 静音片段：标记为已完成（空文本），不报错                     │
│  • 页面保活：最小化时静音音频持续播放                          │
└─────────────────────────┬───────────────────────────────────┘
                          ▼
┌─────────────────────────────────────────────────────────────┐
│              用户点击"结束会议" → 处理完成后                    │
│         ┌──────────────┐  ┌────────────────────┐            │
│         │  生成词云     │  │  查看例图（更大按钮）│            │
│         └──────────────┘  └────────────────────┘            │
└─────────────────────────┬───────────────────────────────────┘
                          ▼
┌─────────────────────────────────────────────────────────────┐
│         合并所有处理结果 → 绿色椭圆词云（带权重）              │
└─────────────────────────────────────────────────────────────┘
```

### 流程B：粘贴文稿生成词云

```
┌─────────────────────────────────────────────────────────────┐
│              用户点击"会议记录"按钮                            │
└─────────────────────────┬───────────────────────────────────┘
                          ▼
┌─────────────────────────────────────────────────────────────┐
│          弹出粘贴框（最多 5000 字，实时字数计数）              │
└─────────────────────────┬───────────────────────────────────┘
                          ▼
┌─────────────────────────────────────────────────────────────┐
│          用户点击"生成词云" → 调用 LLM 权重分析               │
└─────────────────────────┬───────────────────────────────────┘
                          ▼
┌─────────────────────────────────────────────────────────────┐
│              渲染绿色椭圆词云（带权重）                        │
└─────────────────────────────────────────────────────────────┘
```

## API 路由

| 路由 | 方法 | 说明 |
|------|------|------|
| `/api/transcribe` | POST | 音频上传至对象存储 → 签名URL → ASR 转写（静音返回 isSilent） |
| `/api/analyze-words` | POST | 关键词权重分析（LLM 优先，fallback 词频统计） |

## 流水线处理

- **片段时长**：3 分钟
- **最大录音**：2 小时（约 40 个片段）
- **失败处理**：自动重试 1 次，再失败则跳过并显示警告
- **静音片段**：ASR 返回 `[Normal silence audio]` 时，标记为已完成（空文本），不视为错误
- **音频格式**：浏览器 MediaRecorder 录制，优先 `audio/ogg;codecs=opus`，回退到浏览器默认格式
- **音频传输**：FormData 上传 → 对象存储 → 签名 URL → ASR 识别（URL 方式比 base64 更可靠）

## 词云权重规则

| 类别 | 权重倍数 | 示例 |
|------|---------|------|
| 地理相关 | ×3 | 中国、世界、亚洲、城市、河流、山脉 |
| 教育相关 | ×2.5 | 学习、学生、老师、课程、知识 |
| 专业术语 | ×2 | 概念、原理、规律 |
| 一般词汇 | ×1 | 保持原词频 |
| 无意义虚词 | ×0.1 或过滤 | 这个、那个、就是 |

## 构建和测试命令

```bash
# 安装依赖
pnpm install

# 开发环境启动
pnpm dev

# 构建生产版本
pnpm build

# 代码检查
pnpm lint

# 类型检查
pnpm ts-check
```

## 目录结构

```
src/
├── app/
│   ├── page.tsx              # 主页面（含粘贴文稿弹框、例图弹框）
│   ├── layout.tsx            # 布局组件
│   ├── globals.css           # 全局样式（绿色主题）
│   └── api/
│       ├── transcribe/       # ASR 转写 API（对象存储 + 签名URL）
│       └── analyze-words/    # LLM 权重分析 API（LLMClient + fallback）
├── components/
│   ├── MeetingRecorder.tsx   # 录音组件（流水线核心，stop/restart 分片）
│   ├── TranscriptView.tsx    # 转写预览组件
│   └── WordCloud.tsx         # 词云组件
├── hooks/
│   ├── useSpeechRecognition.ts  # 语音识别 hook（保留，未用于核心流程）
│   ├── useSilenceDetection.ts   # 静默检测 hook
│   └── use-mobile.ts            # 移动端适配 hook
└── lib/
    └── utils.ts              # 工具函数

public/
└── wordcloud-example.png     # 词云例图占位（待更换）
```

## 关键配置

### 静默检测配置
- 能量阈值: `0.02` (silenceThreshold)
- 最大静默时间: `30分钟 (1800000ms)` (maxSilenceDuration)

### ASR 配置
- SDK: `coze-coding-dev-sdk` ASRClient
- 音频传输: 对象存储上传 → 签名URL → URL 方式识别
- 静音处理: 返回 `{ text: '', isSilent: true }`

### LLM 配置
- SDK: `coze-coding-dev-sdk` LLMClient
- 模型: `doubao-seed-2-0-lite-260215`
- Fallback: 词频统计 + 停用词过滤

### 词云配置
- 形状: 椭圆 (ellipse)
- 主题色: 绿色系 (#22c55e → #15803d)
- 最大关键词数: 100
- 关键词字数限制: ≤ 5 字

## 响应式适配

| 设备 | 布局 | 字云尺寸 |
|------|------|---------|
| 手机 (< 640px) | 单列，垂直堆叠 | 100% 宽度 |
| 平板 (640-1024px) | 单列，保持比例 | 100% 宽度 |
| 电脑 (> 1024px) | 双栏，左录音右预览 | 固定高度 400px |

## 注意事项

1. **浏览器兼容性**: MediaRecorder API 在 Chrome、Firefox、Safari、Edge 等现代浏览器中可用
2. **HTTPS 要求**: 录音功能需要在安全上下文 (HTTPS) 下运行
3. **麦克风权限**: 用户首次使用时需要授权麦克风访问权限
4. **页面保活**: 录音时最小化也能持续录制（静音音频保活）
5. **对象存储依赖**: ASR 功能依赖对象存储服务上传音频文件
6. **LLM Fallback**: LLM 调用失败时自动降级为本地词频统计，但仍会过滤停用词
