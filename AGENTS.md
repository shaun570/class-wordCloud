# 课堂智析助手 - 项目规范文档

## 项目概览

**项目名称**: 课堂智析助手  
**项目类型**: 跨平台 Web 应用（支持电脑、手机、平板）  
**核心功能**: 课堂录音 → 实时转文字 → 词云 + AI课堂分析 / 粘贴文稿 → 词云 + AI课堂分析

## 技术栈

| 技术 | 说明 |
|------|------|
| Framework | Next.js 16 (App Router) |
| Core | React 19 |
| Language | TypeScript 5 |
| UI 组件 | shadcn/ui + Tailwind CSS |
| 录音 | MediaRecorder API |
| 语音转文字 | ASR API (coze-coding-dev-sdk / ASRClient) |
| 词云权重分析 + 课堂摘要 | LLM API (coze-coding-dev-sdk / LLMClient, doubao-seed-2-0-lite-260215) |
| 对象存储 | S3Storage (coze-coding-dev-sdk), 音频文件上传 + 签名URL |
| 词云 | echarts-wordcloud |

## 核心功能流程

### 流程A：录音转词云 + 课堂分析

```
┌─────────────────────────────────────────────────────────────┐
│              选择学科 → 用户点击"开始录音"                      │
│  • 学科：地理/历史/语文/数学/英语/物理/化学/生物/通用          │
│  • 录音中不可更改学科                                         │
└─────────────────────────┬───────────────────────────────────┘
                          ▼
┌─────────────────────────────────────────────────────────────┐
│                   麦克风开始持续录音                           │
│  • 每 3 分钟保存一个音频片段（stop → onstop收集 → restart）   │
│  • 音频上传至对象存储 → 签名URL → ASR 转写                    │
│  • 后台自动触发 LLM 权重分析                                  │
│  • 最大录音时长：2 小时                                       │
│  • 静默检测：30 分钟无声音自动停止                             │
│  • 静音片段：标记为已完成（空文本），不报错                     │
│  • 页面保活：最小化时静音音频持续播放                          │
└─────────────────────────┬───────────────────────────────────┘
                          ▼
┌─────────────────────────────────────────────────────────────┐
│         用户点击"结束会议" → 所有片段处理完成后                 │
│  ┌──────────────────────┐  ┌──────────────────────┐         │
│  │  生成词云与课堂分析    │  │  导出课堂报告（PDF）  │         │
│  └──────────────────────┘  └──────────────────────┘         │
└─────────────────────────┬───────────────────────────────────┘
                          ▼
┌─────────────────────────────────────────────────────────────┐
│    合并所有处理结果 → 蓝色椭圆词云（带权重）+ AI课堂分析报告    │
│    课堂分析包含：主要话题、教学脉络、反复强调的概念、教学建议    │
└─────────────────────────────────────────────────────────────┘
```

### 流程B：粘贴文稿生成词云 + 课堂分析

```
┌─────────────────────────────────────────────────────────────┐
│              用户点击"会议记录"按钮（Header 右侧）             │
└─────────────────────────┬───────────────────────────────────┘
                          ▼
┌─────────────────────────────────────────────────────────────┐
│          弹出粘贴框（最多 5000 字，实时字数计数）              │
│          显示当前学科，同时生成词云和课堂分析报告              │
└─────────────────────────┬───────────────────────────────────┘
                          ▼
┌─────────────────────────────────────────────────────────────┐
│     用户点击"生成词云 + 课堂分析" → 调用 LLM 权重分析         │
│     + 调用 LLM 课堂摘要生成                                  │
└─────────────────────────┬───────────────────────────────────┘
                          ▼
┌─────────────────────────────────────────────────────────────┐
│         渲染蓝色椭圆词云（带权重）+ AI课堂分析报告             │
└─────────────────────────────────────────────────────────────┘
```

## 页面布局

### 整体结构
- **Header**：左侧 Logo + 应用名称，右侧学科下拉选择器 + "会议记录"按钮
- **Hero Banner**：三步骤引导（录音/转写 → 分析 → 词云）
- **主体区域**（桌面端 2/5 + 3/5 栅格）：
  - 左栏：MeetingRecorder 录音组件
  - 右栏：TranscriptView 转写预览 + WordCloud 词云
- **课堂摘要卡片**：录音结束后展示 AI 课堂分析报告
- **Footer**：项目信息

### 响应式适配

| 设备 | 布局 | 词云尺寸 |
|------|------|---------|
| 手机 (< 1024px) | 单列，垂直堆叠 | 100% 宽度 |
| 电脑 (≥ 1024px) | 2/5 + 3/5 栅格，左录音右预览 | 固定高度 400px |

## 应用状态流转

```
idle（初始）→ recording（点击开始录音）→ generating（生成词云中）→ completed（自动停止）
                                            ↓
                                         idle（重置）
```

- `idle`: 初始状态，学科选择可用
- `recording`: 录音中，学科选择禁用（onRecordingStart 回调触发）
- `generating`: 正在生成词云和课堂分析
- `completed`: 自动停止（2小时/30分钟静默）

## 学科配置

| 学科 | value | 权重特色 |
|------|-------|---------|
| 🌍 地理 | geography | 地理术语权重 ×3 |
| 📜 历史 | history | 历史术语权重 ×3 |
| 📖 语文 | chinese | 文学术语权重 ×3 |
| 📐 数学 | math | 数学术语权重 ×3 |
| 🔤 英语 | english | 英语术语权重 ×3 |
| ⚛️ 物理 | physics | 物理术语权重 ×3 |
| 🧪 化学 | chemistry | 化学术语权重 ×3 |
| 🧬 生物 | biology | 生物术语权重 ×3 |
| 📚 通用 | general | 通用权重规则 |

学科选择器位于 Header 右侧，使用下拉选择器（SubjectDropdown 组件）。

## API 路由

| 路由 | 方法 | 说明 |
|------|------|------|
| `/api/transcribe` | POST | 音频上传至对象存储 → 签名URL → ASR 转写（静音返回 isSilent） |
| `/api/analyze-words` | POST | 关键词权重分析 + 课堂摘要生成（LLM 优先，fallback 词频统计） |

### `/api/analyze-words` 请求参数

| 参数 | 类型 | 说明 |
|------|------|------|
| text | string | 待分析文本 |
| chunkId | string | 片段 ID |
| subject | string | 学科（默认 general） |
| generateSummary | boolean | 是否生成课堂摘要（默认 false） |

### `/api/analyze-words` 响应

```typescript
{
  success: boolean;
  chunkId: string;
  words: { word: string; weight: number }[];
  textLength: number;
  usedFallback: boolean;
  subject: string;
  summary?: {
    mainTopics: string[];
    teachingFlow: string;
    keyConceptsRepeated: string[];
    suggestions: string[];
  } | null;
}
```

## 课堂摘要 (ClassSummary)

| 字段 | 类型 | 说明 |
|------|------|------|
| mainTopics | string[] | 主要话题列表 |
| teachingFlow | string | 教学脉络描述 |
| keyConceptsRepeated | string[] | 反复强调的概念 |
| suggestions | string[] | 教学建议列表 |

课堂摘要触发条件：文本长度 > 50 字 或 已处理片段数 ≥ 3

## 导出课堂报告

- 点击"导出课堂报告（PDF）"按钮触发
- 实现方式：打开新窗口，渲染格式化的课堂报告 HTML，调用 `window.print()`
- 内容包含：词云图片、课堂摘要、转写文本

## 流水线处理

- **片段时长**：3 分钟
- **最大录音**：2 小时（约 40 个片段）
- **失败处理**：自动重试 1 次，再失败则跳过并显示警告
- **静音片段**：ASR 返回 `[Normal silence audio]` 时，标记为已完成（空文本），不视为错误
- **音频格式**：浏览器 MediaRecorder 录制，优先 `audio/ogg;codecs=opus`，回退到浏览器默认格式
- **音频传输**：FormData 上传 → 对象存储 → 签名 URL → ASR 识别（URL 方式比 base64 更可靠）

## 词云权重规则

| 类别 | 权重倍数 | 示例 |
|------|---------|------|
| 学科核心术语 | ×3 | 根据学科动态调整（地理：河流/山脉，数学：函数/方程...） |
| 教育相关 | ×2.5 | 学习、学生、老师、课程、知识 |
| 专业术语 | ×2 | 概念、原理、规律 |
| 一般词汇 | ×1 | 保持原词频 |
| 无意义虚词 | ×0.1 或过滤 | 这个、那个、就是 |

## 构建和测试命令

```bash
# 安装依赖
pnpm install

# 开发环境启动
pnpm dev

# 构建生产版本
pnpm build

# 代码检查
pnpm lint

# 类型检查
pnpm ts-check
```

## 目录结构

```
src/
├── app/
│   ├── page.tsx              # 主页面（Header/Banner/主体/Footer、学科选择、粘贴弹框、课堂摘要卡片）
│   ├── layout.tsx            # 布局组件
│   ├── globals.css           # 全局样式（蓝色主题）
│   └── api/
│       ├── transcribe/       # ASR 转写 API（对象存储 + 签名URL + 静音处理）
│       └── analyze-words/    # LLM 权重分析 + 课堂摘要 API（LLMClient + fallback）
├── components/
│   ├── MeetingRecorder.tsx   # 录音组件（流水线核心，stop/restart 分片，学科感知）
│   ├── TranscriptView.tsx    # 转写预览组件
│   └── WordCloud.tsx         # 词云组件（蓝色主题）
├── hooks/
│   ├── useSilenceDetection.ts   # 静默检测 hook
│   └── use-mobile.ts            # 移动端适配 hook
└── lib/
    └── utils.ts              # 工具函数

public/
└── wordcloud-example.png     # 词云例图占位（待更换）
```

## 关键配置

### 应用状态
- 初始状态: `idle`（学科选择可用）
- 录音中: `recording`（学科选择禁用）
- onRecordingStart 回调: MeetingRecorder 开始录音时通知父组件切换状态

### 静默检测配置
- 能量阈值: `0.02` (silenceThreshold)
- 最大静默时间: `30分钟 (1800000ms)` (maxSilenceDuration)

### ASR 配置
- SDK: `coze-coding-dev-sdk` ASRClient
- 音频传输: 对象存储上传 → 签名URL → URL 方式识别
- 静音处理: 返回 `{ text: '', isSilent: true }`

### LLM 配置
- SDK: `coze-coding-dev-sdk` LLMClient
- 模型: `doubao-seed-2-0-lite-260215`
- Fallback: 词频统计 + 停用词过滤 + 学科权重

### 词云配置
- 形状: 椭圆 (ellipse)
- 主题色: 蓝色系 (#1d4ed8 → #60a5fa)
- 最大关键词数: 100
- 关键词字数限制: ≤ 5 字

## 注意事项

1. **浏览器兼容性**: MediaRecorder API 在 Chrome、Firefox、Safari、Edge 等现代浏览器中可用
2. **HTTPS 要求**: 录音功能需要在安全上下文 (HTTPS) 下运行
3. **麦克风权限**: 用户首次使用时需要授权麦克风访问权限
4. **页面保活**: 录音时最小化也能持续录制（静音音频保活）
5. **对象存储依赖**: ASR 功能依赖对象存储服务上传音频文件
6. **LLM Fallback**: LLM 调用失败时自动降级为本地词频统计，但仍会过滤停用词
7. **学科感知**: 不同学科使用不同的权重配置和 LLM prompt
8. **课堂摘要**: 文本长度 > 50 字或已处理片段数 ≥ 3 时触发摘要生成
9. **Hydration 警告**: 浏览器插件（沉浸式翻译等）可能注入 DOM 属性导致 hydration mismatch 警告，不影响功能
